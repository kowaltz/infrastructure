# Cloud providers for the environment

/*
module "oidc-aws_env" {
    source = "./oidc/aws"

    env = var.env
}
*/

/*
module "providers-cloud_env" {
  source = "../../modules/scalr/providers/terraform-scalr-cloud_env"

  account_id              = var.account_id
  aws_audience            = module.oidc-aws_env.audience
  aws_role_arn            = module.oidc-aws_env.arn
  azure_audience          = "${var.env}-application-oidc_scalr"
  azure_credentials       = {
    client_id = "????????????????????????????????",
    subscription_id = "????????????????????????????????",
    tenant_id = "????????????????????????????????",
  }
  env                     = var.env
  set_of_cloud_providers = local.set_of_cloud_providers
}
*/
